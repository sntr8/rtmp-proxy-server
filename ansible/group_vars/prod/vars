fqdn: stream.kanaliiga.fi
mysql_user: mysql
mysql_password: "{{ vault_mysql_password }}"
mysql_root_password: "{{ vault_mysql_root_password }}"
mysql_database: stream
git_user: stream
git_token: "{{ vault_git_token }}"
image_version: v1.6
config_branch: master
discord_webhook: "{{ vault_discord_webhook }}"
discord_support_group: 

image_versions:
  HAPROXY_VERSION: "$IMAGE_VERSION"
  MYSQL_VERSION: "$IMAGE_VERSION"
  NGINX_HTTP_VERSION: "$IMAGE_VERSION"
  NGINX_RTMP_VERSION: "$IMAGE_VERSION"
  PHP_FPM_VERSION: "$IMAGE_VERSION"
